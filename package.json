{
  "name": "math-quest",
  "private": true,
  "packageManager": "npm@11.16.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "gen:api-types": "poetry run python -m backend.scripts.export_openapi && npm run gen:api-types --workspace @mathquest/web"
  },
  "devDependencies": {
    "turbo": "^2.9.16"
  },
  "overrides": {
    "postcss": "^8.5.15"
  }
}
